<UserControl x:Class="PetSitters.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="560" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="28"
                Width="440" Margin="0,24" VerticalAlignment="Top" HorizontalAlignment="Center"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Create your account" Style="{StaticResource H1}"/>
                <TextBlock Text="Tell us who you are. You can update details later."
                           Foreground="{StaticResource Muted}" TextWrapping="Wrap"/>

                <TextBlock Text="I want to..." Style="{StaticResource FieldLabel}"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="OwnerRadio" Content="Find a sitter (Owner)" IsChecked="True"
                                 GroupName="Role" Margin="0,0,18,0"/>
                    <RadioButton x:Name="SitterRadio" Content="Offer sitting (Sitter)" GroupName="Role"/>
                </StackPanel>

                <TextBlock Text="Full name" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="NameBox"/>

                <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="EmailBox"/>

                <TextBlock Text="Password (min 6 characters)" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="PasswordBox"/>

                <TextBlock Text="Phone" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="PhoneBox"/>

                <TextBlock Text="Location (suburb / city)" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="LocationBox"/>

                <TextBlock x:Name="ErrorText" Foreground="{StaticResource Danger}" TextWrapping="Wrap"
                           Margin="0,10,0,0" Visibility="Collapsed"/>

                <Button Content="Create account" Margin="0,18,0,0" Click="Register_Click" IsDefault="True"/>

                <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                    <TextBlock Text="Already have an account? " Foreground="{StaticResource Muted}"/>
                    <Button Content="Log in" Style="{StaticResource LinkButton}" Click="BackToLogin_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
