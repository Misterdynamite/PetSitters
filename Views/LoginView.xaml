<UserControl x:Class="PetSitters.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="900">
    <Grid>
        <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="28"
                Width="380" VerticalAlignment="Center" HorizontalAlignment="Center"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Welcome back" Style="{StaticResource H1}"/>
                <TextBlock Text="Log in to find or offer pet sitting." Foreground="{StaticResource Muted}"/>

                <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="EmailBox"/>

                <TextBlock Text="Password" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="PasswordBox"/>

                <TextBlock x:Name="ErrorText" Foreground="{StaticResource Danger}" TextWrapping="Wrap"
                           Margin="0,10,0,0" Visibility="Collapsed"/>

                <Button Content="Log in" Margin="0,16,0,0" Click="Login_Click" IsDefault="True"/>

                <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                    <TextBlock Text="New here? " Foreground="{StaticResource Muted}"/>
                    <Button Content="Create an account" Style="{StaticResource LinkButton}" Click="Register_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
